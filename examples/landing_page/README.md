# Landing Page Example - KD UI Framework

This example demonstrates the **landing page generation** capabilities of the KD UI Framework MCP server.

## 🎯 What This Example Shows

This landing page was **100% generated** by the `create_landing_page()` MCP tool. No hand-coding, no manual HTML/CSS - just pure tool-generated output!

## 🚀 Features Demonstrated

### 1. Hero Section
- Large, bold headline with gradient background
- Compelling subtitle
- Two CTA buttons (primary and secondary)
- Lucide icons integrated
- Fully responsive

### 2. Features Grid
- 6 features in responsive 3-column layout
- Icon badges with blue background
- Hover effects on cards
- Clean, minimal design

### 3. Testimonials
- Customer quotes with 5-star ratings
- Avatar circles with initials
- Name, role, and company information
- Gray background for visual separation

### 4. Pricing Cards
- 3 pricing tiers (Open Source, Pro, Enterprise)
- "Popular" badge on recommended tier
- Feature lists with checkmark icons
- Hover effects and blue border on popular plan

### 5. Call-to-Action Banner
- Full-width gradient blue background
- Compelling headline and subtitle
- White button that pops
- Arrow icon for direction

### 6. Professional Footer
- Multi-column layout
- Company info and description
- Product and company links
- Social media icons (Twitter, GitHub, LinkedIn)
- Copyright notice

## 🎨 Design Quality

All components follow **Shadcn design principles**:
- ✅ Inter font for clean typography
- ✅ Lucide icons throughout
- ✅ Blue-600 primary color
- ✅ Subtle gray backgrounds (gray-50, gray-100)
- ✅ White cards with subtle borders
- ✅ Generous padding (p-6, p-8, p-12)
- ✅ Smooth hover transitions
- ✅ Shadow-sm for depth
- ✅ Fully responsive grid layouts

## 🔧 How to Run

1. **Navigate to this directory:**
   ```bash
   cd examples/landing_page
   ```

2. **Run the Flask app:**
   ```bash
   python app.py
   ```

3. **Open your browser:**
   ```
   http://localhost:5002
   ```

## 📝 The Source Code

### app.py
Simple Flask application that:
1. Imports the `create_landing_page()` function
2. Calls it to generate the complete HTML
3. Returns it as the response

That's it! The tool does all the heavy lifting.

### How the Tool Was Called

```python
from kd_ui_server.tools.landing_page import create_landing_page

html = create_landing_page(
    title="KD UI Framework - Beautiful UI Components for Flask",
    company_name="KD UI Framework"
)
```

## 🎯 Key Takeaway

**This entire landing page was generated by the MCP tool!**

- No manual HTML writing
- No CSS styling needed
- No design decisions required
- Just call the function and get a production-ready landing page

This proves the MCP server can generate **Shadcn-quality** landing pages automatically!

## 🔄 Customization

You can customize the landing page by passing different configurations to each component:

```python
from kd_ui_server.tools.component import add_component

# Custom hero
hero = add_component("hero", {
    "title": "Your Custom Title",
    "subtitle": "Your custom subtitle",
    "cta_primary": "Custom CTA",
    "cta_secondary": "Learn More"
})

# Custom features
features = add_component("features", {
    "features": [
        {
            "icon": "zap",
            "title": "Fast",
            "description": "Lightning speed"
        },
        # ... more features
    ]
})

# And so on...
```

## 📚 Components Used

This landing page uses these MCP-generated components:
- `add_component("navbar", {...})`
- `add_component("hero", {...})`
- `add_component("features", {...})`
- `add_component("testimonials", {...})`
- `add_component("pricing", {...})`
- `add_component("cta", {...})`
- `add_component("footer", {...})`

## ✨ Try It Yourself!

Ask Cline or Claude:

```
Using the create_landing_page MCP tool, generate a landing page for [your product]
```

The tool will create a complete, professional landing page instantly!

---

**Generated with ❤️ by KD UI Framework MCP Server**
