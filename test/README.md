# Test Directory

This directory contains test files and resources used during development and testing.

## Structure

- `screenshots/` - Screenshots captured during Playwright testing and UI validation
- `test_minimal_buttons.html` - Test HTML file for button component validation
- `pico-*.md` - Documentation and research files for component migration

## Note

This directory is excluded from version control via `.gitignore` as it contains temporary test artifacts and development resources.
